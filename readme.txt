###connection request aceept login

request param:
   => status(accepted or rejected);
      => validate if the status recieved from request is accepted or recieved if anything throw the error
      
2. 
